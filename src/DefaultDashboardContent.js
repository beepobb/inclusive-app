import React from 'react';
import Card from './Card';

const cardData = {
    title: "Card Title",
    content: "This is some example content for the card.",
    imageUrl: "https://via.placeholder.com/300x200" // Example image URL
};

export default function DefaultDashboardContent() {
    return (
        <div>
        <h1>Dashboard</h1>
        </div>

    );
}
